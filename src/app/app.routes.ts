import { Routes } from '@angular/router';
import { NumPadComponent } from './numpad';

import { DataResolver } from './app.resolver';

export const ROUTES: Routes = [

  { path: '', component: NumPadComponent }
];
